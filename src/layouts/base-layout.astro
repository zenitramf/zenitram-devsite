---
import SiteFooter from "@/components/layout/site-footer.astro";
import SiteHeader from "@/components/layout/site-header.astro";
import "@/styles/starwind.css";

interface Props {
  description?: string;
  title?: string;
}

const {
  title = "Zenitram Solutions — Technology for the local church",
  description = "Church-focused technology: websites, graphic design, media, staff tools, and upcoming Zenith Cast.",
} = Astro.props;
---

<!doctype html>
<html class="dark" lang="en">
  <head>
    <meta charset="utf-8">
    <link href="/favicon.svg" rel="icon" type="image/svg+xml">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <meta content={Astro.generator} name="generator">
    <meta content="dark" name="color-scheme">
    <meta content="#111111" name="theme-color">
    <title>{title}</title>
    <meta content={description} name="description">
    <link href="https://fonts.googleapis.com" rel="preconnect">
    <link crossorigin href="https://fonts.gstatic.com" rel="preconnect">
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    >
  </head>
  <body class="min-h-svh bg-background font-sans text-foreground antialiased">
    <a
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:rounded-full focus:bg-primary focus:px-4 focus:py-2 focus:text-primary-foreground"
      href="#main"
    >
      Skip to main content
    </a>
    <SiteHeader />
    <main id="main">
      <slot />
    </main>
    <SiteFooter />
  </body>
</html>
